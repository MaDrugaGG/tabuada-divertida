{
  "name": "tabuada-divertida",
  "version": "1.0.0",
  "description": "App de tabuada para criancas",
  "scripts": {
    "build": "echo Web app pronto",
    "setup": "npm install && npx cap add android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "canvas": "^3.2.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
